image: node:14.15-alpine

pages:
  stage: deploy
  script:
  - node build.js > _sidebar.md
  - mkdir .public
  - cp -r * .public
  - mv .public public
  artifacts:
    expire_in: 1 day
    paths:
    - public
  only:
  - master
